# 変更履歴

このファイルの形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [0.2.0] - 2026-08-08

### 追加

- サムネイルサイズ設定に「最大」を追加。パネル幅いっぱいに1列で表示します（対応8言語すべてに文言を追加）。

### 変更

- 拡大表示の「リンクを開く」「名前変更」「閉じる」ボタンと画像名を、マウスカーソルが画像上にあるときだけ表示するようにしました。カーソルを外すと画像全体が見えます。
- サムネイルの縦横比を3:2に固定。パネル幅を変えても比率が変わらなくなりました。
- サムネイルサイズ設定を実サイズ基準に変更。パネルを広げると1枚が大きくなるのではなく表示枚数が増えます。小/中/大/最大の違いが常に明確になりました。
- 一覧の表示に縮小版（WebP・長辺640px）を使うようにしました。原寸画像は拡大表示を開いたときにのみ読み込みます。
- 一覧の追加読み込みをスクロール連動に変更。シェルフ内の全画像を先読みしなくなりました。

### 修正

- `chrome.windows.onRemoved` のリスナーが5重に登録されていた問題を修正。
- 保存のたびに、購読していないコンテンツスクリプトへ画像データ（数MB）を送信していた問題を修正。
- ウィンドウ位置やキー設定など表示に関係しないストレージ変更でも、UI全体を作り直していた問題を修正。
- `pointermove` などのイベントリスナーを全ページ・全フレームに常時登録していた問題を修正。ドラッグ中のみ登録します。
- シェルフ名をエスケープせずに `innerHTML` へ挿入していた問題を修正。

### その他

- 0.1.0 以前に保存した画像の縮小版は、初回表示時に生成してデータベースへ保存します。古いシェルフを最初に開くときだけ、わずかに時間がかかります。

## [0.1.0]

- 初回リリース。
